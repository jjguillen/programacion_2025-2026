package tema3.ejerciciosarrays.masejercicios;

public class GuardiasSeguidos {
    static void main() {

        //Dado un array de booleans que representan guardias (true) y no guardias (false),
        //escribe un programa que determine si hay al menos tres guardias seguidos en el array.
        //Utiliza una función que reciba el array y devuelva true o false.
        //Ojo: el array puede tener cualquier tamaño y te puedes pasar al comprobar

        boolean[] guardias = {true, true, false, true, true, true, false, true};

    }
}
